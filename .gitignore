/.idea/
/build
*__pycache__/
/main.spec
/logs/
# generated PDFs in output (not diffable - JSON outputs are tracked)
output/**/*.pdf
# intermediate key_mapping (raw .keys + integer debug PDFs)
/key_mapping/
# debug PDFs in fields_mapping (field-name overlays for visual verification)
fields_mapping/**/*.pdf
